// Script for handling interactive elements (like the grammar details)
document.addEventListener('DOMContentLoaded', () => {
    // Current script is minimal since resizing was removed.
    console.log("Ivrit project loaded");
});
